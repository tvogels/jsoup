# Jsoup with Source Tracking

This is a fork of [Jsoup](http://jsoup.org/) that record of the original position of nodes in the source document. Positions are available via public properties

* `.startPosition` and
* `.endPosition`.